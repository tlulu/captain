apiVersion: v1
kind: Service
metadata:
  name: captain
spec:
  type: NodePort
  selector:
    app: captain
  ports:
    - name: http
      port: 80
      targetPort: 3000
      # Setting nodePort for local dev.
      nodePort: 30007